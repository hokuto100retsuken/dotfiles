# コミットメッセージ例

## 良い例

```
feat(meta-ads): アカウント連携一覧画面を追加

運用担当者が申請済みのMeta広告アカウントを確認できるようにする
```

```
fix(php): EUC-JPファイルの文字化けを修正

直接編集していたためnkfワークフローに従い修正
```

## 避ける例

- `修正しました` → What/Why 不明
- `Update MetaAdAccountTest.php` → 日本語で、Why 欠如
